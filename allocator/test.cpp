#include "pool_allocator.h"
#include <vector>
#include <list>
#include <unordered_map>

struct test
{
    int a;
    int_fast64_t p;
};

int main() {
    std::<> v {1, 2, 3, 4, 5};
    // v.push_back(7);
    for (auto &&i : v)
    {
        std::cout << i << " ";
    }  
}