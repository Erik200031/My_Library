#include <iostream>
#include <functional>
// #include "unordered_map.h"
#include <vector>
#include <unordered_map>

#include "../allocator/pool_allocator.h"
#include <initializer_list>

struct test
{
    void test_t() {std::cout << "hello";}
};






auto main() {

    // mylib::unordered_map<int, int> l;
    // l.insert(mylib::make_pair(11, 22));
    // l.insert(mylib::make_pair(10, 2));
    // l.insert(mylib::make_pair(131, 22));
    // l.insert(mylib::make_pair(140, 2));
    // l.insert(mylib::make_pair(151, 22));
    // l.insert(mylib::make_pair(1110, 2));
    // l[10] = 7;
    // l.print();
    return int(0);
}